import React, { Component } from 'react';
import {
    TouchableHighlight,
    StyleSheet,
    Text
} from 'react-native';
import Button from './button';

class StopButton extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <Button onPress={this.props.onPress} textStyle={styles.textStyle} text={'Stop'} />
        );
    }
}

const styles = StyleSheet.create({
    textStyle: {
        color: 'red'
    }
});

export default StopButton;