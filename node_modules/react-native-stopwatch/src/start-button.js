import React, { Component } from 'react';
import {
    TouchableHighlight,
    StyleSheet,
    Text
} from 'react-native';
import Button from './button';

class StartButton extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <Button onPress={this.props.onPress} textStyle={styles.textStyle} text={'Start'} />
        );
    }
}

const styles = StyleSheet.create({
    textStyle: {
        color: '#69d66f'
    }
});

export default StartButton;