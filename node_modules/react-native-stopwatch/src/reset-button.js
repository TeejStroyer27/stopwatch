import React, { Component } from 'react';
import {
    TouchableHighlight,
    StyleSheet,
    Text
} from 'react-native';
import Button from './button';

class ResetButton extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <Button onPress={this.props.onPress} textStyle={styles.textStyle} text={'Reset'} />
        );
    }
}

const styles = StyleSheet.create({
    textStyle: {}
});

export default ResetButton;